<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.spring.mvc.board.repository.IBoardMapper">

<!-- 
	VO멤버변수와 DB의 컬럼명이 같으면
	굳이 resultMap쓰지 않아도 동작한다.
 -->
	<resultMap type="com.spring.mvc.board.model.BoardVO" id="BoardMap"> 
		<id property="boardNum" column="board_no"/>
		<result property="regDate" column="reg_date" />
		<result property="viewCnt" column="view_cnt" />
	</resultMap>

	<insert id="insert">
		INSERT INTO mvc_board
		(board_no,title,content,writer)
		VALUES(board_seq.NEXTVAL,#{title},#{content},#{writer})
	</insert>
	<select id="getArticleList" resultMap="BoardMap">
		SELECT * FROM mvc_board ORDER BY board_no DESC
	</select>
	<select id="getArticle" resultMap="BoardMap">
		SELECT * FROM mvc_board
		WHERE board_no = #{boardNum}
	</select>
	
	<update id="update">
		UPDATE mvc_board SET title = #{title}, content=#{content}
		WHERE board_no = #{boardNum}
	</update>
	
	<delete id="delete">
		DELETE FROM mvc_board WHERE board_no = #{boardNum}
	</delete>
	
	
</mapper>